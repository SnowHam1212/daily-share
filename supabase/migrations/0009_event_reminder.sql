-- ============================================================
-- 0009_event_reminder.sql
-- 予定にリマインダー（開始の何分前に通知するか）を保存する列を追加。
--
-- NULL = リマインダーなし。値は「開始の N 分前」を表す分数
-- （例: 0=開始時刻ちょうど, 10=10分前, 1440=1日前）。
-- 配信はクライアント側（アプリ起動中のブラウザ通知）で行う。
-- 将来 Web Push（#50）を導入したらサーバー配信に拡張する。
-- ============================================================

ALTER TABLE events ADD COLUMN IF NOT EXISTS "reminderMinutes" integer;
